attack:
    target_type: machine
    target_config:
        blast_radius: 100
        hostnames:
            - mockhost.resilience.yahoo.cloud
        ssh_config: {}
    agents:
        -   type: contrib
            config:
                path: "path/to/dummy_agent.py"
